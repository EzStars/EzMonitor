# Changesets

欢迎使用 Changesets！这个文件夹由 `@changesets/cli` 自动生成，用于管理 `@ezmonitor/sdkv2` 包的版本控制和发布。

> **当前配置**: 只有 `@ezmonitor/sdkv2` 包参与版本管理，其他包被忽略。

## 📦 如何使用

### 1. 添加变更记录
```bash
# 交互式添加变更记录
pnpm changeset:add

# 或者直接使用
pnpm changeset
```

### 2. 查看变更状态
```bash
pnpm changeset:status
```

### 3. 版本升级
```bash
pnpm changeset:version
```

### 4. 发布包
```bash
pnpm changeset:publish
```

### 5. 一键发布（推荐）
```bash
pnpm release
```

## 🔄 工作流程

1. **开发功能** → 编写代码
2. **添加 Changeset** → `pnpm changeset:add`
3. **提交代码** → git commit & push
4. **自动化发布** → GitHub Actions 会自动处理版本升级和发布

## 📋 变更类型

- **major**: 重大更新，包含破坏性变更
- **minor**: 新功能，向后兼容
- **patch**: Bug 修复，向后兼容

## 🚀 自动化

项目配置了 GitHub Actions，当推送到 `main` 分支时会：
1. 自动创建 Release PR（如果有待发布的变更）
2. 合并 PR 后自动发布到 npm

更多信息请参考 [Changesets 官方文档](https://github.com/changesets/changesets)
