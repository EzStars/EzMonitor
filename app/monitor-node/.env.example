# MongoDB 配置
MONGODB_URI=mongodb://localhost:27017/ezmonitor

# 服务器配置
PORT=3000
NODE_ENV=development

# CORS 配置（多个域名用逗号分隔）
CORS_ORIGINS=http://localhost:5173,http://localhost:5174

# 鉴权配置
AUTH_JWT_SECRET=replace_with_strong_jwt_secret
AUTH_ACCESS_TOKEN_TTL_SEC=900
